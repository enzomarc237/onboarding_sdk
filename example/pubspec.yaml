name: my_feature_tour_example
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish this package.

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  onboarding_sdk:
    path: ../
  shared_preferences: ^2.0.0 # Using a recent version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true